on: [push]

jobs:
    deploy_job:
    runs-on: ubuntu-latest
    name: deploy
    steps:
        - name: Checkout
        uses: actions/checkout@v2
        - name: deploy file
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
            username: ${{ secrets.FTP_USERNAME }} 
            server: ${{ secrets.FTP_HOST }} 
            port : ${{ secrets.FTP_PORT }} 
            password : ${{ secrets.FTP_PASWORD }} 
            local_path: './*'
            remote_path: '/chronica/txData/QBCoreFramework_BA9C00.base/resources/selim-drifts'
            sftpArgs: '-o ConnectTimeout=5'
